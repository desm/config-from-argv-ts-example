{
  "name": "config-from-argv-ts-example",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -fr ./dist/ && tsc && chmod +x ./dist/example.js",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config-from-argv": "^1.0.2"
  },
  "devDependencies": {
    "@sindresorhus/tsconfig": "^5.0.0",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
